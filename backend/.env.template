# Base de datos (Neon / Postgres)
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_SCHEMA=classroom

# CORS - orígenes permitidos separados por coma
CORS_ORIGINS=http://localhost:5173

# Secreto para firmar tokens de sesión (cambiar en producción)
TOKEN_SECRET=atv-classroom-dev-secret

# Agente RAG
TOP_K=3
ANTHROPIC_API_KEY=
